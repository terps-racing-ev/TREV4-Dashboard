trev dash changes
GOAL: Multi dbc per bus
BACKEND
- [ ] Support DBC priority. Ie. prefer the highest dbc if theres a conflict
- [ ] Support upload of multiple dbcs to backend
    - [ ] ADD dbc endpoint(bus, dbc)
    - [ ] Remove dbc endpoint(bus, dbc)

FRONTEND
- [ ] Add dropdown component instead of SWAP  button
    - [ ] Enumerate all the dbcs per bus
    - [ ] X button 
